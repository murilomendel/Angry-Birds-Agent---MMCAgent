import os # Use of operating system
import sys
from stat import *
from random import randint

if __name__ == "__main__":
    flag = randint(1, 99)
    #flag = 65
    print("In Python - Target Zone = " + str(flag))
    print(str(flag))